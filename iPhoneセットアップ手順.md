# 📱 iPhoneセットアップ手順書

このアプリを iPhone にインストールしてジムで使えるようにする手順です。

---

## ✅ ステップ概要

```
[Mac] アプリのファイル一式
    ↓
  [GitHubにアップロード（プライベート不可・パブリック必須）]
    ↓
  [GitHub Pages を有効化]
    ↓
  [iPhoneのSafariで公開URLを開く]
    ↓
  [ホーム画面に追加] → 完了！
```

> ⚠️ **重要**: GitHub Pages は **パブリックリポジトリ** でのみ無料で使えます。  
> 個人の運動記録なので公開しても困らないと思いますが、URLを知らない人にはアクセスされにくいです（とはいえ完全な秘密にはなりません）。
> もし完全プライベートにしたい場合は、後述の「代替案」を参照してください。

---

## 📋 詳細手順

### ステップ 1: GitHubに新しいリポジトリを作成する

1. ブラウザで https://github.com を開いてログイン
2. 右上の「+」→「New repository」
3. 設定：
   - **Repository name**: `gym-record`（任意の名前）
   - **Public** を選択（GitHub Pages の無料枠のため）
   - 「Add a README file」はチェックしなくてOK
4. 「Create repository」クリック

### ステップ 2: ファイルをアップロード

ターミナルを開いて以下を実行（`<USERNAME>` をご自身のGitHubユーザー名に置き換え）：

```bash
cd ~/Documents/ClaudeCode\ 自作プログラム保管場所/フィットネス記録ツール
git init
git add index.html manifest.json icon-192.png icon-512.png
git commit -m "初回アップロード"
git branch -M main
git remote add origin https://github.com/<USERNAME>/gym-record.git
git push -u origin main
```

> 📝 既存ツールでGitHub設定済みなので、既に `gh auth` 済みのはずです。  
> もし認証エラーが出たら `gh auth login` で再認証してください。

### ステップ 3: GitHub Pages を有効化

1. GitHubで作成したリポジトリページを開く
2. 上部メニュー「**Settings**」をクリック
3. 左サイドバーの「**Pages**」をクリック
4. 「Source」を「**Deploy from a branch**」に設定
5. Branch: 「**main**」、フォルダ: 「**/ (root)**」を選択
6. 「Save」をクリック
7. 1〜2分待つと、上部に「Your site is live at https://<USERNAME>.github.io/gym-record/」と表示される

### ステップ 4: iPhoneのSafariでアクセス

1. iPhoneの **Safari** を開く（Chromeではなく Safari がおすすめ）
2. アドレスバーに上記URLを入力（例: `https://<USERNAME>.github.io/gym-record/`）
3. アプリが表示される

### ステップ 5: ホーム画面に追加（重要）

1. Safariで開いた状態で、画面下中央の **共有ボタン**（□に↑のアイコン）をタップ
2. メニューを下にスクロール → 「**ホーム画面に追加**」をタップ
3. 名前を確認して「**追加**」をタップ
4. ホーム画面に「ジム記録」アイコンが追加される
5. **以後はこのアイコンから起動** → 普通のアプリのように全画面で動作します

---

## 💪 使い方

### 記録入力
1. アプリを起動 → 「記録入力」タブ（デフォルト表示）
2. 種別を選ぶ：マシン / 有酸素 / 自重
3. 種目をプルダウンから選択
4. 数値を入力
5. 「記録する」ボタン

### 便利機能
- **前回値をコピー**: 同じ種目の前回入力値をワンタップで埋められます
- **前回記録の自動表示**: 種目を選ぶと、前回の重量・回数と「自己ベスト」が表示されます
- **今日の記録**: 入力画面下に今日入力した一覧が表示されます

### 履歴タブ
- 種別・種目でフィルタして過去記録を確認

### 設定タブ
- 種目リストの追加・削除（自分のジムのマシン名を登録）
- **CSVエクスポート**: Macに記録を移すときに使用 → メール添付やAirDropで送る
- **JSONバックアップ**: 全データのバックアップ

---

## 🔄 Mac へのデータ移行方法

iPhoneのデータはSafariのlocalStorage内にあるため、Macとの自動同期はできません。  
以下の方法でMacへ移せます：

### 方法A: CSV → メール
1. iPhoneアプリで「設定」タブ → 「CSVでエクスポート」
2. 共有メニューから「メール」で自分宛に送信
3. Macで受信 → Excel/Numbersで開く

### 方法B: AirDrop
1. iPhoneでCSVエクスポート → 共有メニューから「AirDrop」で自分のMacへ
2. Macの「ダウンロード」フォルダにCSVが届く

---

## ⚠️ 注意事項

- **データの保存場所**: iPhoneのSafariブラウザ内（localStorage）
  - Safariの履歴を「すべて削除」するとデータも消えます
  - 定期的に「JSONバックアップ」を取ることを推奨
- **iPhone と Mac で別データになります**（同じURLを開いても、それぞれの端末に独立保存）
- 機種変更時は、旧iPhoneでJSONバックアップ → 新iPhoneで「バックアップから復元」

---

## 🔒 完全プライベートにしたい場合（代替案）

GitHub Pages のパブリック公開が嫌な場合：

### 代替案A: ローカルファイルとしてiPhoneに入れる
- iCloud Drive にHTMLファイルを置く → iPhoneのファイルアプリから開く
- ただし「ホーム画面に追加」での全画面化は不可

### 代替案B: Netlify Drop（無料・ログイン不要・URLは推測困難）
1. https://app.netlify.com/drop を開く
2. このフォルダ全体をドラッグ＆ドロップ
3. ランダムなURL（例: `https://random-words-12345.netlify.app`）が発行される
4. このURLをiPhoneのSafariで開く
   - 推測困難なURLなので実質プライベート

代替案B を希望される場合は、お知らせください。

---

## 🆘 トラブルシューティング

### Q. ホーム画面に追加してもブラウザのアドレスバーが残る
A. 「Safariで開く」になっていませんか？「ホーム画面に追加」ボタンから追加し直してください。

### Q. データが消えた
A. Safariの設定で「サイトデータを削除」したり、長期間（数か月）アプリを開かないと消える場合があります。  
  → 月1回は JSON バックアップをエクスポートしておくと安心です。

### Q. iPhoneとMacで記録を同期したい
A. 現在の構成では自動同期できません。CSVエクスポートで手動移行をお願いします。  
  Googleスプレッドシート連携での自動同期版が必要であれば、お知らせください（要追加開発）。
