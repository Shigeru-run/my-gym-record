# 📱 iPhoneセットアップ手順書

GitHubにアプリをアップロード済みなので、**iPhoneでの設定だけ**で使えます。

---

## 🌐 アプリのURL

```
https://shigeru-run.github.io/my-gym-record/
```

> ⚠️ 初回デプロイは GitHub Pages のビルドに 1〜2 分かかります。  
> アクセスして「404 Not Found」が出る場合は、少し待ってから再度開いてください。

---

## 📲 iPhoneセットアップ（5ステップ・2分で完了）

### ステップ 1: SafariでURLを開く

iPhoneの **Safari**（Chromeではなく Safari）を開き、アドレスバーに以下を入力：

```
https://shigeru-run.github.io/my-gym-record/
```

> 📝 一度開いたら、ブックマークしておくと次回が楽です。

### ステップ 2: ホーム画面に追加

1. 画面下中央の **共有ボタン**（□に↑のアイコン）をタップ
2. メニューを下にスクロール → 「**ホーム画面に追加**」をタップ
3. 名前は「ジム記録」のまま「**追加**」をタップ
4. ホーム画面に「ジム記録」アイコンが追加されます

### ステップ 3: アプリとして起動

ホーム画面の「ジム記録」アイコンをタップ → 全画面でアプリのように起動します（アドレスバーなし・通常のアプリと同じ見た目）。

### ステップ 4: 試しに1件記録してみる

1. **記録入力**タブで「マシン」を選ぶ
2. 種目から「チェストプレス」など選択
3. 重量・回数・セット数を入力 → 「記録する」をタップ
4. 同じ画面下に「今日の記録」として表示されることを確認

### ステップ 5: 完了！

ジムに行ったらこのアプリで記録するだけ。
2回目以降、同じ種目を選ぶと**前回の値が自動で入力欄に表示**されます。

---

## 💪 使い方ガイド

### 🔵 4つの記録カテゴリ

| 種別 | 記録項目 |
|------|---------|
| **マシン** 🏋️ | 種目 / 重量(kg) / 回数 / セット数 |
| **有酸素** 🏃 | 種目 / 時間(分) / 距離(km、任意) |
| **自重** 🤸 | 種目 / 回数 / セット数 |
| **ストレッチ** 🧘 | 完了として記録するだけ（時間・回数なし） |

### ✨ 便利な機能

- **前回値の自動表示**: 種目を選ぶと前回の値が自動で入力欄に入ります
- **自己ベスト表示**: マシン種目は最大重量が表示されるので、前回より重く挙げられたか一目瞭然
- **「前回値をコピー」ボタン**: 一度クリアした入力欄に再度前回値を埋めたい時に使用
- **履歴フィルタ**: 種別・種目で絞り込み表示
- **マシンアイコン**: 部位が分かるアイコン表示（💪🦵🚣🍑など）

### ⚙️ 設定タブでできること

- **種目リストの編集**: 自分のジム特有のマシン名を追加 / 不要な種目を削除
- **CSVエクスポート**: Macで集計したい時に使う（メール添付やAirDropで送る）
- **JSONバックアップ**: 月1回くらい取っておくと安心
- **バックアップから復元**: 機種変更時や誤操作後の復旧用

---

## 🔄 Mac へのデータ移行方法

iPhoneとMacは**別データ**で動きます（自動同期なし）。記録をMacで分析したい場合は：

### 方法A: CSV → メール
1. iPhoneアプリの「設定」タブ → 「**CSVでエクスポート**」
2. 共有メニューから「メール」で自分宛に送信
3. Macで受信 → Excel/Numbersで開く

### 方法B: AirDrop
1. iPhoneでCSVエクスポート → 共有メニューから「**AirDrop**」で自分のMacへ
2. Macの「ダウンロード」フォルダにCSVが届く

CSV列構成:
| 日付 | 種別 | 種目 | 重量(kg) | 回数 | セット数 | 時間(分) | 距離(km) | 備考 |
|------|------|------|---------|------|---------|---------|---------|------|

---

## ⚠️ 注意事項

- **データ保存場所**: iPhoneのSafariブラウザ内（localStorage）
  - Safariの「履歴をすべて削除」やサイトデータ削除をするとデータも消えます
  - 月1回 JSONバックアップ をエクスポートして iCloud Drive に保存することを推奨
- **iPhone と Mac は別データ**: それぞれの端末に独立して保存されます
- 機種変更時: 旧端末でJSONバックアップ → 新端末で「バックアップから復元」

---

## 🔧 アプリのアップデート手順（開発者向けメモ）

修正・機能追加を行った場合：

```bash
cd ~/Documents/ClaudeCode\ 自作プログラム保管場所/フィットネス記録ツール
git add -u
git commit -m "修正内容の説明"
git push
```

push後 1〜2分で GitHub Pages が自動更新されます。  
iPhoneで反映を確認したい場合は、Safariで一度ページをリロード（下に引っ張る）してください。

---

## 🆘 トラブルシューティング

| 症状 | 対処 |
|-----|-----|
| ホーム画面アイコンから起動するとアドレスバーが残る | 「Safariで開く」になっています。一度削除して、「ホーム画面に追加」から追加し直してください |
| データが消えた | Safari設定でサイトデータが削除された可能性。月1回 JSONバックアップを推奨 |
| ボタンが反応しない | Safariをいったん終了して再起動してください |
| 404 Not Found が出る | 初回デプロイ中。1〜2分待ってから再度アクセス |

---

## 📂 リポジトリ

- GitHub: https://github.com/Shigeru-run/my-gym-record
- 公開ページ: https://shigeru-run.github.io/my-gym-record/
